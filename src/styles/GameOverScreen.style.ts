import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    fontWeight: 'bold',
    fontSize: 18,
    marginBottom: 10
  },
  totalRounds: {
    marginBottom: 10
  }
});
