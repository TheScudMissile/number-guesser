import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center'
  },
  card: {
    width: 300,
    maxWidth: '80%',
    alignItems: 'center',
    marginTop: 100
  }
});
